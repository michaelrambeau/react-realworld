import React from 'react'
import PropTypes from 'prop-types'

const Header = props => {
  return (
    <header>
      <h1>Real World Demo</h1>
    </header>
  )
}

Header.propTypes = {}

export default Header
